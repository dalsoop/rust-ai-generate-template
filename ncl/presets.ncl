# 도메인 프리셋 — 자주 쓰는 조합을 이름으로 묶음
{
  presets = {
    minimal = {
      description = "hello만",
      domains = ["hello"],
    },
    full = {
      description = "모든 도메인",
      domains = ["hello", "ping"],
    },
  },
}
